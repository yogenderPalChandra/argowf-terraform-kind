apiVersion: argoproj.io/v1alpha1
kind: EventSource
metadata:
  name: bitbucket-repo
  namespace: argo-events
spec:
  service:
    ports:
      - port: 12000
        targetPort: 12000
  webhook:
    bitbucket-webhook:
      port: "12000"
      endpoint: /github
      method: POST